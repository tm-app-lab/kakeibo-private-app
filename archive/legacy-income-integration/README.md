# Legacy income integration archive

This folder keeps the previous standalone/iframe-era payroll implementation for reference only.

The active app no longer loads these files. The current income management screen is rendered natively from `app.js`, using seed data from `data/payroll-data.js`.
